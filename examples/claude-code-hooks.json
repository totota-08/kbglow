{
  "//": "Merge the 'hooks' section below into ~/.claude/settings.json. Replace $HOME with your actual home directory if your shell does not expand it.",
  "hooks": {
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "grep -qi permission && ($HOME/.local/bin/kbglow pulse --blink --period 2 -t 600 >/dev/null 2>&1 &) || true"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "hooks": [
          { "type": "command", "command": "$HOME/.local/bin/kbglow stop >/dev/null 2>&1 || true" }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          { "type": "command", "command": "$HOME/.local/bin/kbglow stop >/dev/null 2>&1 || true" }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          { "type": "command", "command": "$HOME/.local/bin/kbglow stop >/dev/null 2>&1 || true" }
        ]
      }
    ]
  }
}
